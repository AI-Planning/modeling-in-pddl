(define (problem hello-world)
    (:domain hello-world)

    ; TODO: define the `:objects` section and list the neighbors

    (:init
    )

    (:goal
        ; TODO: modify the goal, so both neighbors are greeted
        (said_hello)
    )
)